---
import Layout from '../layouts/layout.astro';
const pageTitle = 'Sobre mi';

const identity = {
  firstName: 'Cristian',
  country: 'España',
  occupation: 'Frontend Developer',
  hobbies: ['deporte', 'playa', 'informatica']
};

const skills = [
  'HTML',
  'CSS',
  'JavaScript',
  'React',
  'Vue',
  'Astro',
  'Boostrap',
  'SASS',
  'jQuery',
  'Wordpress',
  'Dart',
  'Flutter',
  'Git',
  'GitHub',
  'Figma'
];

const happy = true;
const finished = false;
const goal = 7;

const skillColor = 'navy';
const fontWeight = 'bold';
const textCase = 'uppercase';
---

<Layout title={pageTitle}>
  <h1>{pageTitle}</h1>
  <h2>Hecho con Astro...</h2>

  <p>
    Este es el blog que estoy haciendo con el tutorial oficial de <a
      target='_blank'
      href='https://docs.astro.build/es/tutorial/2-pages/1/'>Astro</a
    >
  </p>

  <p>
    Este sitio se irá actualizando a medida que vaya completando más partes del
    tutorial, ¡así que no dejes de visitarlo para ver cómo va mi viaje!
  </p>

  <p>He aqui algunos datos sobre mi:</p>

  <ul>
    <li>Me llamo {identity.firstName}</li>
    <li>Vivo en {identity.country} y trabajo como {identity.occupation}.</li>
    {
      identity.hobbies.length >= 2 && (
        <li>
          Dos de mis aficiones son: {identity.hobbies[0]} y{' '}
          {identity.hobbies[1]}
        </li>
      )
    }
  </ul>

  <p>Mis habilidades son:</p>
  <ul>
    {skills.map(skill => <li class='skill'>{skill}</li>)}
  </ul>

  {happy && <p>¡Estoy feliz de aprender Astro!</p>}

  {finished && <p>¡He terminado este tutorial!</p>}

  {
    goal === 3 ? (
      <p>Mi objetivo es terminar en 3 días.</p>
    ) : (
      <p>Mi objetivo no son 3 días.</p>
    )
  }
</Layout>

<style define:vars={{ skillColor, fontWeight, textCase }}>
  h1 {
    color: purple;
    font-size: 4rem;
  }
  .skill {
    color: var(--skillColor);
    font-weight: var(--fontWeight);
    text-transform: var(--textCase);
  }
</style>
